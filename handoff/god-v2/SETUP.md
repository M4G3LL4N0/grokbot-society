# SETUP

Preparation only. Do not execute a live interaction yet.

1. Create one fresh GrokBot named **God**.
2. Supply only the exact contents of `GOD_PROMPT.txt` as its system prompt.
3. Connect the operator to Society through `CONTRACT.json`; keep the bridge in
   dry mode and use only the supplied bounded scene package.
4. Run `pnpm society god health` once, verify the limits, then stop.

Do not reuse the archived God, generate a social circle, run a paid test, or
perform repository work.
